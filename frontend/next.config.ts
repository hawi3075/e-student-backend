// next.config.ts

import type { NextConfig } from 'next'; // <-- MUST import the type

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Your config options
};

export default nextConfig;
